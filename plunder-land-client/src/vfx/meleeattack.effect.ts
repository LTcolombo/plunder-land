import TWEEN from '@tweenjs/tween.js'
import { Sprite, Texture, Loader, Graphics } from 'pixi.js'

export class MeleeAttackEffect {
  constructor (owner, lifetime) {

    // var sheet = Loader.shared.resources["./res/atlas.json"];
    // var tex = [];
    // for (let frame of sheet.data.animations["player/attack"])
    //     tex.push(Texture.from(frame));

    // owner.animation.textures = tex;
    // owner.animation.animationSpeed = 0.2;
    // owner.animation.loop = false;
    // owner.animation.play();

    // setTimeout(() => {

    //     tex = [];
    //     for (let frame of sheet.data.animations["player/walk"])
    //         tex.push(Texture.from(frame));

    //     owner.animation.textures = tex;
    //     owner.animation.loop = true;
    //     owner.animation.play();
    // }, 400);
  }
}
